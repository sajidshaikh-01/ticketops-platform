replicaCount: 1 # initial value only; ignored once autoscaling.enabled is true
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
image:
  repository: 674182809289.dkr.ecr.us-east-1.amazonaws.com/ticketops-dev-admin-api
  tag: "1973713"
service:
  port: 80
  targetPort: 3000
envFromSecret: ticketops-db-secret
env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: "3000"
  - name: CORS_ORIGIN
    value: "https://dev.sajid-platform.online"
ingress:
  enabled: true
  path: /api/admin
  groupName: ticketops-dev
  groupOrder: 10
healthCheck:
  enabled: true
  path: /api/admin/health
  port: 3000

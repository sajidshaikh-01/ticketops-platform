apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticketops-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ticketops-service.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
